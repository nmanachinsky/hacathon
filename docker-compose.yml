services:
  scanner:
    build: .
    image: pii-scanner:latest
    container_name: pii-scanner
    volumes:
      - ./data/share:/data:ro
      - ./reports:/reports
    environment:
      WORKERS: "${WORKERS:-10}"
      OCR_BACKEND: "${OCR_BACKEND:-tesseract}"
      OMP_THREAD_LIMIT: "1"
    command:
      - scan
      - --input
      - /data
      - --output
      - /reports
      - --workers
      - "${WORKERS:-10}"
      - --ocr
      - "${OCR_MODE:-auto}"
